
for(int divider=1; divider <= n; ++divider)  // for loop 
{                                            // start of the loop block 
    if(n % divider == 0) 
        std::cout << divider << ", "; 
}                                            // end of the loop block